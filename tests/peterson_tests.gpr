project Peterson_Tests is

   for Main use ("peterson_tests.adb");
   for Source_Dirs use (".");
   for Object_Dir use "../obj";
   for Exec_Dir use "../bin";
   
   package Compiler is
      for Switches ("Ada") use ("-g", "-O2", "-gnatwa");
   end Compiler;

end Peterson_Tests;
